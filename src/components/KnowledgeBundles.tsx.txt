import AnimatedSection from "./AnimatedSection";

const bundles = [
  {
    title: "Real Estate Investor Intelligence Bundle",
    description: "Essential readings for property investors seeking rigorous analytical frameworks and market insight.",
    bookCount: 4,
  },
  {
    title: "Built Environment Design Bundle",
    description: "Architecture and interior design volumes for practitioners shaping physical spaces.",
    bookCount: 3,
  },
  {
    title: "Entrepreneur & Builder Bundle",
    description: "Strategic guides for founders, operators, and those building enterprises from the ground up.",
    bookCount: 3,
  },
  {
    title: "Complete DreamCap Knowledge Library",
    description: "The full collection — every volume across all disciplines, assembled as a unified resource.",
    bookCount: 12,
  },
];

const KnowledgeBundles = () => (
  <AnimatedSection className="px-6 md:px-[10%] py-24 md:py-32 bg-muted" id="bundles">
    <p className="text-sm tracking-[0.3em] uppercase text-secondary mb-6 font-body">
      Collections
    </p>
    <h2 className="text-3xl md:text-4xl font-display font-light mb-16">
      Knowledge Bundles
    </h2>
    <div className="grid grid-cols-1 md:grid-cols-2 gap-px bg-border">
      {bundles.map((bundle) => (
        <div key={bundle.title} className="bg-background p-8 md:p-12 flex flex-col">
          <h3 className="text-xl md:text-2xl font-display font-normal mb-4 leading-snug">
            {bundle.title}
          </h3>
          <p className="text-sm font-body text-muted-foreground leading-relaxed mb-2">
            {bundle.description}
          </p>
          <p className="text-xs font-body tracking-[0.2em] uppercase text-secondary mb-8">
            {bundle.bookCount} volumes
          </p>
          <button className="self-start mt-auto text-sm font-body tracking-wide border border-foreground px-6 py-3 hover:bg-foreground hover:text-background transition-colors duration-500">
            Purchase Bundle
          </button>
        </div>
      ))}
    </div>
  </AnimatedSection>
);

export default KnowledgeBundles;
