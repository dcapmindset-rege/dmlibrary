import AnimatedSection from "./AnimatedSection";

const themes = [
  {
    title: "Capital",
    books: [
      "Strategic Property Analysis",
      "Capital & Property",
      "The Real Estate Portfolio",
      "Market Cycle Intelligence",
    ],
  },
  {
    title: "Design",
    books: [
      "Foundations of Modern Architecture",
      "Interior Spaces",
      "Material & Form",
      "The Spatial Framework",
    ],
  },
  {
    title: "Sustainability",
    books: [
      "Sustainable Living Design",
      "Regenerative Systems",
      "The Green Building Mandate",
      "Conscious Development",
    ],
  },
];

const ReadingList = () => (
  <AnimatedSection className="px-6 md:px-[10%] py-24 md:py-32 border-t border-border" id="reading-list">
    <p className="text-sm tracking-[0.3em] uppercase text-secondary mb-6 font-body">
      Curated
    </p>
    <h2 className="text-3xl md:text-4xl font-display font-light mb-16">
      DreamCap Investor Reading List
    </h2>
    <div className="grid grid-cols-1 md:grid-cols-3 gap-px bg-border">
      {themes.map((theme) => (
        <div key={theme.title} className="bg-background p-8 md:p-10">
          <h3 className="text-2xl font-display font-normal mb-6 italic">
            {theme.title}
          </h3>
          <div className="w-8 h-px bg-secondary mb-6" />
          <ul className="space-y-4">
            {theme.books.map((book) => (
              <li
                key={book}
                className="text-sm font-body text-muted-foreground leading-relaxed pl-0 border-b border-border pb-4 last:border-0"
              >
                {book}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </AnimatedSection>
);

export default ReadingList;
